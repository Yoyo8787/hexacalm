# 角色規格與 Commit 計畫

狀態：產品決策已確認，功能尚未實作。本文件定義本批實作範圍與驗收條件；commit 清單為拆分計畫，不代表已建立或授權立即建立 commit。

產品總體規格見 [SPEC.md](../SPEC.md)，用語見 [CONTEXT.md](../CONTEXT.md)。

## 模型與動作

- 使用 [Kenney Cube Pets](https://kenney.nl/assets/cube-pets)，首批為貓、小雞、狗、豬、牛；排除猴子。
- 叫聲分別挑選輕喵、輕啾、輕聲短叫、柔和鼻哼、低沉短哞；音檔需試聽，避免尖銳、連續或驚嚇感強的聲音。
- 模型、預覽與聲音需保留來源及授權記錄。Cube Pets 不附音效，需另外取得。
- 優先沿用模型內建動畫；若不符合需求，再自行控制共用動作。
- 行走呈現向前小跳，轉彎平順轉向，停止後待機。
- 按下停止時立即停止水平前進，原地平順落地；重新開始時從原處繼續。

## 角色生命週期

- 世界最多一隻角色。
- 新角色在有效道路的最大群組中出生，預設直接開始行走。
- 最大群組以有效道路 Tile 數量判斷；同大小時隨機選一組，再從組內隨機選出生道路。
- 加入角色、重新安置與讀取存檔後的出生均使用相同選路規則。
- 孤立的有效道路允許加入角色。無相鄰連通道路時原地待機，保留行走啟用狀態；補通道路後自動繼續。
- 使用者手動停止後，補通道路不會自動開始。
- 更換角色保留位置、方向、速度與移動狀態。
- Build／Relax 切換不影響角色狀態。
- 目前位置仍有效時留在原處；其他群組變大不會觸發傳送。
- 所在道路失效時重新安置，保留角色種類、速度與行走啟用狀態；世界中沒有任何有效道路時移除角色。
- 無道路而移除角色後，重建道路不會自動重新加入角色。

## 道路移動與歷史

- 僅通過旋轉後雙向出口相通的可通行道路；既有橋樑、河流及建築不因本批功能自動變成可通行。
- 岔路隨機選路，優先排除來路；沒有其他方向時允許折返。完全無連通方向時待機。
- 格內行走路徑需沿道路模型銜接入口與出口，包含彎道；只連接 Tile 中心不足以完成此需求。
- 速度以滑桿控制，預設 1×，範圍 0.5×–2×。1× 初始調整目標為相鄰直道路格中心之間約兩秒；彎道依路徑長度行走，避免瞬間加速。
- 速度影響移動與步頻，不影響叫聲間隔或環境音播放速度。
- 角色操作不加入建造歷史。Undo／Redo 不還原角色種類、位置、行走啟用狀態或速度，也不復活已移除的角色。
- Add、Replace、Rotate、Remove、Undo、Redo 造成的道路變化，均重新檢查角色位置與可行路徑；這是適應目前道路，不是復原角色歷史。
- 道路修改後，目前位置仍有效就原地續行並重算路徑；失效時套用重新安置或移除規則。

## 存檔與恢復

- 角色持久化只包含角色種類及行走啟用狀態；無角色時記錄空值。
- 不儲存角色位置、方向、格內進度、跳躍姿勢、速度、視角或叫聲倒數。
- 重新進入世界時，依最大道路群組規則重新出生，恢復行走啟用狀態；速度回到 1×，視角回到 Builder View。
- 恢復已啟用行走的角色時，有路就走，無連通方向就待機；若完全沒有有效道路則移除角色。
- 加入、更換、移除角色及手動開始／停止時儲存；不因每幀移動持續寫入 LocalStorage。
- 世界 Tile、Build／Relax 模式與既有音訊設定仍依現有規則儲存。
- 實作新增角色欄位時同步調整資料型別、驗證與世界版本；開發期間不做舊格式相容，版本不符仍清除存檔並安全回到空白狀態。
- 關閉網頁期間不計算角色行走進度。

## 角色聲音

- 叫聲使用低音量短音檔，隨機間隔 20–45 秒，不重疊播放。
- 停止行走時仍會偶爾叫；更換角色時停止舊叫聲，重新計算間隔。
- 加入角色、恢復世界、恢復音訊播放或解除靜音後，重新等待 20–45 秒再播放叫聲。
- 暫停或靜音期間不累積待播放叫聲，恢復後不補播。
- 所有角色共用輕柔腳步聲，每次行走落地觸發，配合速度與步頻。
- 音訊暫停、靜音與主音量控制全部聲音，不影響角色移動。
- 不新增叫聲、腳步聲各自的音量控制。
- 移除角色時取消叫聲排程及角色音效；離開世界頁面時停止全部聲音並清理播放資源。

## 背景分頁

- 分頁進入背景時，暫停角色實際移動與腳步聲，保留行走啟用狀態。
- 環境音與間隔叫聲維持播放，仍受主音量、靜音與音訊暫停控制。
- 回到前景時依原行走啟用狀態繼續，不補跑背景期間距離，不集中補播延遲的叫聲。
- 瀏覽器可能限制背景計時或暫停分頁，不保證背景叫聲精準準時；恢復時避免累積觸發。

## Character Listener

- 納入本批，以獨立 commit 處理。
- 只要有角色，就以角色位置與朝向決定環境音，與 Builder／第一／第三人稱視角無關。
- 近處 Tile 聲音較清楚，遠處逐漸淡出；初始有效距離以約三格為調整起點，實際衰減需試聽。
- 角色方向影響左右聲道，移動與轉向時平順更新，避免聲音跳變。
- 角色停止仍持續播放局部環境音；重新安置後平順過渡到新位置的混音。
- 移除角色時平順回到 World Listener；仍有角色的重新安置不切回 World Listener。

## 角色鏡頭

- 提供第三人稱與第一人稱視角，平滑切換並跟隨角色方向。
- 不提供自由轉頭；依總體規格，不提供跟隨距離控制或碰撞避讓。
- 鏡頭高度保持穩定，避免隨小跳上下震動。
- 移除角色時回到 Builder View。
- 建造操作限定 Build Mode 且使用 Builder View；第一／第三人稱隱藏建造介面與可放置空格，保留角色、音訊、模式與視角控制。
- 切回 Builder View 後才可編輯道路；切換視角不改變角色行走啟用狀態。

## Commit 拆分與驗收

先整理本次規格文件，再依以下順序實作。每個 commit 僅包含該階段必要變更，並同步 README 現況。實作與建立 commit 均需另行啟動；測試需使用者明確要求才執行。

| 順序 | 建議 commit 主旨                   | 依賴 | 驗收條件                                                                                                                                                           |
| ---- | ---------------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 0    | docs: 定義角色規格與分階段驗收     | 無   | 總體規格、角色規格與詞彙一致，沒有未決產品問題。                                                                                                                   |
| 1    | feat: 加入五種寵物模型與預覽資產   | 0    | 貓、小雞、狗、豬、牛模型與預覽路徑有效；確認比例、朝向與內建動畫，保留授權。                                                                                       |
| 2    | feat: 加入角色生命週期與精簡存檔   | 1    | 可加入、更換、移除；正確選最大群組與處理平手；無道路不可加入；存檔只包含角色種類與行走啟用狀態，重入重新選路；升版不相容；角色操作不進 Undo／Redo。                |
| 3    | feat: 加入角色共用動作控制         | 2    | 優先使用原動畫；五種角色皆能銜接行走、平順轉向與待機；停止時不懸空；提供落地時機供後續腳步聲使用。                                                                 |
| 4    | feat: 加入沿道路行走與速度控制     | 3    | 直路、彎道、岔路、死路與旋轉出口均正確；速度與停止生效；孤立道路補通後依啟用狀態續行；全部道路操作含 Undo／Redo 後不走進無效道路、不倒帶角色；背景返回不跳躍補跑。 |
| 5    | feat: 加入寵物叫聲與共用腳步音檔   | 1    | 五種叫聲與共用腳步聲試聽通過，音量平衡且不尖銳，檔案來源與授權完整。                                                                                               |
| 6    | feat: 整合角色叫聲排程與落地音效   | 4、5 | 20–45 秒間隔、不重疊、不補播；叫聲與腳步聲遵守主音量、靜音和暫停；步頻與落地一致；更換、移除及離頁不殘留舊聲音或排程。                                             |
| 7    | feat: 加入角色位置與朝向的環境混音 | 4、6 | 靠近／遠離 Tile、轉向、停止及重新安置時聲音正確且連續；與視角無關；移除角色後回到世界混音。                                                                        |
| 8    | feat: 加入角色跟隨鏡頭與視角切換   | 4、7 | 第一／第三人稱平順跟隨，無小跳震動；無角色不可使用；移除角色回 Builder View；只有 Build Mode 的 Builder View 可建造，視角切換不中斷角色或聲音。                    |

階段 2 先建立行走啟用狀態，實際沿道路移動在階段 4 完成；Character Listener 在階段 7 完成前暫用既有世界混音，角色鏡頭在階段 8 才提供。這些是分階段缺口，不是最終行為。

## 實作時的驗證方式

- 每階段檢查 diff、相關資產路徑與存檔／歷史邊界；使用既有 Prettier 格式化修改檔案。
- 動畫、聲音、步頻、鏡頭與距離衰減須透過實際預覽或試聽確認；本次規格討論尚未完成這些驗證。
- 依上表情境人工驗收。需要自動化測試或型別／靜態檢查時，另依專案授權規則執行。
- 隨機世界生成、Sleep Timer、更多角色、多角色與手動移動不屬於本批範圍。
